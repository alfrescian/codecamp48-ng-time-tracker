'use strict';

describe('Service: administrationService', function () {

  // load the service's module
  beforeEach(module('fireTimeTracker'));

  // instantiate service
  var administrationService;
  beforeEach(inject(function (_administrationService_) {
    administrationService = _administrationService_;
  }));

  // it('should do something', function () {
  //   expect(!!administrationService).toBe(true);
  // });

});
